# web

TV H5。工作区若只打开本目录，以本文件为准；若打开上一级工程根，以根目录 `CLAUDE.md` 为准。

- 页面：`apps/shell/src/pages/`，路由：`apps/shell/src/router/index.ts`
- `<EPage id default-focus>`，`@enter`，不要 `@click`，不要直连 `@chancestv/tv-focus`
- 用法：`docs/tv-ui.md`；写页 skill：`chances-tv-ui`
- 换肤：`apps/shell/src/theme.css`

```bash
pnpm install && pnpm dev
```
