// 模块分层（命名前缀即层级，Studio 视图按字母序天然呈现 app→业务→base）：
// app（壳应用）→ feature_*（业务层）→ lib_*（基础库）
// 语音聚合在 feature_voice/ 下：core=抽象稳定层，shijiu=可热插拔视九实现（不用时删 :feature_voice:shijiu 这项 + app runtimeOnly，见其 README）
include ':app', ':feature_voice:core', ':feature_voice:shijiu', ':lib_base'
